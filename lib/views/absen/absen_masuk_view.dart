import 'package:flutter/material.dart';

class AbsenMasukView extends StatelessWidget {
  const AbsenMasukView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ini panel masuk'),
    );
  }
}
