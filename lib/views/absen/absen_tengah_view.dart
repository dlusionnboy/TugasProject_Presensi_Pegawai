import 'package:flutter/material.dart';

class AbsenTengahView extends StatelessWidget {
  const AbsenTengahView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('halaman tengah'),
    );
  }
}
