import 'package:flutter/material.dart';

class AbsenKeluarView extends StatelessWidget {
  const AbsenKeluarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('halaman keluar'),
    );
  }
}
