import 'package:flutter/material.dart';

class RiwayatProvider extends StatelessWidget {
  const RiwayatProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
