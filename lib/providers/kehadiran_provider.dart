import 'package:flutter/material.dart';

class KehadiranProvider extends StatelessWidget {
  const KehadiranProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
